<div id="votes">
  <% if @total_votes > 0%>
    <% @votes.each do |v| %>
      <div class="result"><%= (100 * v.votes/@total_votes).round %>% (<%= v.votes %> ratings)</div>
    <% end %>
    <% else %>
    <p>No-one has rated yet!</p>
  <% end %>
</div>