
let myPromise = new Promise(function(myResolve, myReject) {
  $("#countrycontent").html("<%= escape_javascript(render 'country') %>");
  myResolve(function(){
     $( "select option:selected" ).text("<%= @name %>")
  myReject();  // when error
});
