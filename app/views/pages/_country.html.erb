
<% if @government %>

<% if @carers %>
  <h1 style="padding-bottom: 0.5em;">
<% else %>
      <h1>
      <% end %>
      How was your customer experience with the government of
        <select name="country_select" id="country_select">
      <option  data-fullname="<%= @government.name%>" value="<%= @government.country_code.downcase%>" selected="selected"><%= @government.name %> </option>
      <% @other_countries.each do |oc| %>
          <option data-fullname="<%= oc.name %>" value="<%= oc.country_code %>"><%= oc.url_string %> </option>
      <% end %>
    </select>
    <div id="flagcontainer">
    <%= image_tag("flags/#{@government.country_code.downcase}.svg", :class=> "flag", :alt => "#{@government.name} flag") %></div>
        today?</h1>
<div id = "heroes">
<% if @carers %>
<%= image_tag("click.svg", :alt => "click symbol", :class => "clickleft") %>
 <%= image_tag("click.svg", :alt => "click symbol", :class => "clickright") %>

 
        <h2 style="text-align: center; font-size: 2em; margin:0;">PLEASE CLICK FOR OUR HEROES!</h2>
        <p style="text-align:center; padding-bottom: 2em; margin: 0;">(Or <a style="color: black;" href="https://members.parliament.uk/member/4070/contact">ask the government</a> to support NHS staff in a meaningful way by paying them properly.)</p>
<% end %>
</div>
<div id="achievements"></div>

        <div id="voteButtons">

    <% (1..4).each do |x| %>
    <div id="vote<%= x %>" class="voteButton" data-disabled="false" data-govid="<%= @government.country_code %>" data-govrecordid="<%= @government.id %>">
      <%= image_tag("#{x}.svg", :alt => @alt_texts[x - 1]) %>
    </div>
    <% end %>
  </div>
  <div id="results" data-historyscores="<%= @history_scores.to_json%>">
    <%= render "votes" %>
  </div>
 

  
  
<% else %>
  <h1>Nothing to see here!</h1>
<% end %>